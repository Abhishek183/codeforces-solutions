#include<iostream>
#include<string.h>
using namespace std;
 void take(char str)
{   int k=length(str);
    for(int j=k-3;j<=k);j++)
    {  char str2;
        str2=cin.get(j);
    }
}

int length(char str)
   { int i;
       for(int i=0;str!='\0';i++); return i;
   }
 void cpy(char str2)
 {
     take(str);
       cout<<str2.append(str)
 }
int main()
   {   char str;
       cin.get(ch);
       cpy(str2);
       cout<<str2;
   }
}

